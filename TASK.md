# TASK: 온보딩 드롭다운 필수 에러 표시

## 목표
연매출/월광고예산 Select 필드에 미선택 시 에러 안내 표시

## 빌드/테스트
- npm run build 성공 필수
- 테스트: 수강생 가입 후 온보딩에서 드롭다운 안 고르고 저장 시도

## T1. Select 필드 에러 표시

### 파일
- `src/app/(auth)/onboarding/page.tsx`

### 현재 동작
- 연매출/월광고예산 Select를 안 고르면 버튼만 disabled
- 왜 비활성인지 사용자에게 안내 없음

### 기대 동작
- submit 시도 시(또는 blur 시) 미선택 Select 아래에 빨간색 "필수 항목입니다" 텍스트 표시
- 브랜드명/쇼핑몰URL input도 동일하게 blur 시 에러 표시
- 기존 disabled 로직은 유지

### 하지 말 것
- 온보딩 전체 흐름 변경 금지
- 기존 T3 구현(disabled 조건) 건드리지 말 것
- 새 UI 컴포넌트 추가 금지

## 리뷰 결과
(에이전트팀 리뷰 후 기록)
