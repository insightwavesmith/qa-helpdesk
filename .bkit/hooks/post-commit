#!/bin/bash
# post-commit — git post-commit hook
# pdca-chain-handoff.sh만 호출 (알림 통합)
# 항상 exit 0 (hook 실패가 커밋을 막지 않음)

DIR="$(cd "$(dirname "$0")" && pwd)"

bash "$DIR/pdca-chain-handoff.sh" 2>/dev/null &

exit 0
