# 타겟중복율 — Plan

> 작성일: 2026-02-25
> 의존: TASK-총가치각도기 완료 (✅ 커밋 8f72f09)

## 목적
Meta 광고 계정 내 활성 광고세트 간 타겟 중복률을 분석하여 광고비 낭비를 시각화

## 범위
- T1: `adset_overlap_cache` 테이블 생성 (RLS 포함)
- T2: `/api/protractor/overlap` GET API (캐시 + Meta API + 중복 계산)
- T3: `OverlapAnalysis` 컴포넌트 + 대시보드 탭 구조

## 성공 기준
- 7일 이상 기간 선택 시 중복률 도넛 + 위험 경고 + 세트 테이블 렌더링
- 캐시 HIT 시 Meta API 미호출
- "새로 분석" 버튼으로 캐시 무시 재분석 가능
- `npm run build` 성공, lint 0
