# B3. AI 프롬프트 humanize — 설계서

> 작성: 2026-03-02
> 참조: T5(t5-content-ai-prompt.design.md), route.ts 현재 systemPrompt

## 1. 데이터 모델
- 해당 없음 (DB 변경 없음)

## 2. API 설계
- 해당 없음 (API 엔드포인트 변경 없음, 프롬프트 텍스트만 수정)

## 3. 컴포넌트 구조

### 3-1. systemPrompt 문체 규칙 추가

**파일**: `src/app/api/admin/curation/generate/route.ts`

**현재 "작성 규칙" 섹션** (약 84~152줄):
```
## 작성 규칙
- ~해요 체 (부드럽고 친근하지만 전문적)
- 한국어 기본. 영어 전문용어는 한글(영어) 병기 (첫 등장만, 이후 한글)
- 숫자로 말하고 스토리로 풀기 — 데이터가 먼저, 감성이 뒤따름
- 문단 짧게 (2~3문장)
- 소제목 넘버링 필수
- 섹션 사이 구분선(---) 필수
- 추측 표현 금지 ("~인 것 같아요")
- 교과서적 정의로 시작 금지 ("~란 ~입니다")
- 각 h2 섹션마다 > 인용문 최소 1개 필수
- 각 h2 섹션마다 ![이미지 설명](IMAGE_PLACEHOLDER) 최소 1개 필수
- ## 마치며 섹션은 반드시 포함. 생략 금지
```

**수정 후** — 기존 항목 유지 + 문체 humanize 규칙 블록 추가:

```
## 작성 규칙
- ~해요 체 (부드럽고 친근하지만 전문적)
- 한국어 기본. 영어 전문용어는 한글(영어) 병기 (첫 등장만, 이후 한글)
- 숫자로 말하고 스토리로 풀기 — 데이터가 먼저, 감성이 뒤따름
- 문단 짧게 (2~3문장)
- 소제목 넘버링 필수
- 섹션 사이 구분선(---) 필수
- 추측 표현 금지 ("~인 것 같아요")
- 교과서적 정의로 시작 금지 ("~란 ~입니다")
- 각 h2 섹션마다 > 인용문 최소 1개 필수
- 각 h2 섹션마다 ![이미지 설명](IMAGE_PLACEHOLDER) 최소 1개 필수
- ## 마치며 섹션은 반드시 포함. 생략 금지

## 문체 규칙 (자연스러운 글쓰기 — 반드시 지키기)

### 어미 다양화 (같은 어미 3번 연속 금지)
- 허용 어미: ~요, ~다, ~죠, ~거든요, ~네요, ~데요, ~ㄹ까요, ~답니다
- 예시: "성과가 달라져요. 실제로 그렇다. 왜냐면 알고리즘이 학습하거든요."
- 금지: "~했어요. ~봤어요. ~됐어요." (같은 어미 3연속)

### 문장 리듬
- 짧은 문장(15자 이내)과 긴 문장(40자+)을 번갈아 배치
- 예시: "결과가 나왔다. 3주간 ASC 캠페인을 돌린 수강생의 ROAS가 기존 대비 2.4배 올랐거든요."
- 금지: 모든 문장이 비슷한 길이로 나열

### 의문문·감탄문 활용
- 각 h2 섹션마다 의문문 또는 감탄문 1~2개 삽입
- 예시: "왜 이런 차이가 날까요?" "진짜 됩니다." "어떻게 하면 될까요?"

### 구어체 전환어
- 문단 시작에 전환어를 적절히 사용
- 허용: "사실", "솔직히", "그런데 말이죠", "재미있는 건", "여기서 포인트는", "한 가지 더"
- 금지: 모든 문단이 주어+서술어로 시작

### AI 상투어 금지 목록
절대 사용 금지 표현:
- "매우 중요합니다", "매우 효과적입니다"
- "필수적입니다", "핵심입니다", "핵심적인"
- "반드시 ~해야 합니다"
- "~하는 것이 중요합니다"
- "~할 수 있습니다"
- "주목할 만한", "놀라운"
- "획기적인", "혁신적인"
대체 표현 예시:
- "매우 중요합니다" → "이건 진짜 차이를 만들어요" 또는 "여기서 갈린다"
- "필수적입니다" → "이거 안 하면 손해예요" 또는 "꼭 해보세요"
- "반드시 ~해야 합니다" → "~해보면 알게 될 거예요"

### 경험담 톤
- "수업에서도 자주 나오는 질문인데요" 같은 교육 현장 멘트 삽입
- "실제로 수강생 중에 ~한 분이 계셨는데" 같은 사례 화법
- 1인칭("제가 보기에", "저희 수강생 중")을 적절히 사용
- 마치 강의실에서 설명하는 것처럼 쓰기
```

### 3-2. 변경 위치 상세

systemPrompt 문자열 내에서, 기존 `## 작성 규칙` 섹션 바로 뒤 (현재 `## 마크다운 이스케이프 규칙` 앞)에 `## 문체 규칙` 블록을 삽입한다.

```
기존 순서:
## 핵심 원칙
## 글 구조 (리캐치 패턴)
## 글자수 (절대 규칙)
## 작성 규칙              ← 기존 유지
## 문체 규칙              ← 여기에 NEW 블록 삽입
## 마크다운 이스케이프 규칙
## 강의 컨텍스트 활용법
## 출력 형식
```

### 3-3. TYPE_PROMPTS.education과의 관계

- `route.ts`의 systemPrompt = 큐레이션(외부 트렌드 기반) 콘텐츠 생성용
- `contents.ts`의 TYPE_PROMPTS.education = 교육 콘텐츠 생성용 (T5에서 별도 수정 완료)
- 이번 B3는 `route.ts` systemPrompt만 수정
- TYPE_PROMPTS.education에도 같은 문체 규칙을 적용하고 싶다면 별도 태스크 필요 (이번 범위 외)

## 4. 에러 처리
- 프롬프트 변경이므로 런타임 에러 없음
- AI가 규칙을 완벽히 따르지 않을 수 있음 → 결과물 검토 필요
- 기존 생성된 콘텐츠에는 영향 없음 (새로 생성하는 콘텐츠에만 적용)

## 5. 구현 순서
- [ ] `src/app/api/admin/curation/generate/route.ts` — systemPrompt 내 `## 작성 규칙` 뒤에 `## 문체 규칙` 블록 삽입
- [ ] 기존 `## 작성 규칙` 항목 변경 없음 확인
- [ ] 글 구조(훅→도입→목차→본론→마치며) 변경 없음 확인
- [ ] 글자수 규칙(4,000자 이상) 변경 없음 확인
- [ ] `npm run build` 성공 확인

## 6. 변경 요약

| 항목 | Before | After |
|------|--------|-------|
| 어미 다양화 | 규칙 없음 | 같은 어미 3연속 금지, 허용 어미 목록 |
| 문장 리듬 | 규칙 없음 | 짧은/긴 문장 번갈아 |
| 의문문/감탄문 | 규칙 없음 | 섹션당 1~2개 |
| 구어체 전환어 | 규칙 없음 | 허용 전환어 목록 |
| AI 상투어 | 규칙 없음 | 금지 목록 + 대체 표현 |
| 경험담 톤 | 규칙 없음 | 교육 현장 멘트, 사례 화법 |

## 7. 영향 범위

| 파일 | 변경 유형 | 위험도 |
|------|----------|--------|
| `src/app/api/admin/curation/generate/route.ts` | systemPrompt 텍스트 추가 | 낮음 |

- 변경 파일: 1개
- AI 모델: 변경 없음 (gemini-2.5-flash-preview-05-20)
- RAG 로직: 변경 없음
- 글 구조: 변경 없음
- 기존 콘텐츠: 영향 없음
