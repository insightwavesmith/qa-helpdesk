# B1/B2/T2-fix/T4-fix Gap 분석 — authError 처리 + inactive 삭제 + QA 프롬프트 수정

> 작성: 2026-03-05
> QA Engineer

## 전체 Match Rate: 100%

---

## B1. signup/page.tsx authError 처리 수정 — Match Rate: 100%

### 일치 항목

- **authError 조건 변경**: L144 `if (authError && !authData?.user)` — 기존 `if (authError)` 에서 `!authData?.user` 조건이 추가됨. authError가 있어도 유저가 생성됐으면 에러를 무시하고 정상 플로우 진행. (설계서 3-B1, TASK.md B1 일치)
- **authData null-safe 접근**: L149 `if (!authData?.user)` — 기존 `if (!authData.user)` 에서 optional chaining(`?.`)이 추가됨. authError로 인해 authData가 null일 수 있는 케이스를 안전하게 처리. (설계서 3-B1 일치, 추가 안전성 개선)
- **B1 주석 표기**: L143 `// B1: authError가 있어도 유저가 실제 생성됐으면 정상 플로우 진행` — 변경 이유를 코멘트로 명시. (추적성 확보)
- **에러 처리 로직 (authError + user 미존재)**: L144-147에서 authError가 있고 user도 없을 때만 `setError(authError.message)` + `return`. (설계서 4-B1 에러처리 일치)
- **정상 플로우 (authError + user 존재)**: authError가 있더라도 `authData?.user`가 존재하면 L149 이후 파일 업로드, 초대코드 처리, 리다이렉트 로직으로 정상 진행. (설계서 4-B1 에러처리 일치)

### 금지 사항 검증

- **Supabase Auth 설정 변경**: 없음 (코드 변경만)
- **middleware.ts 수정**: `git diff main --name-only` 결과에 middleware.ts 없음. 미수정 확인.
- **가입 플로우 전체 구조 변경**: `handleSignup` 함수의 전체 흐름(signUp → 파일업로드 → 초대코드 → 리다이렉트) 유지. 조건문 2줄만 변경.

### 불일치 항목

- 없음

### 수정 필요

- 없음

---

## B2. admin.ts inactive 삭제 허용 — Match Rate: 100%

### 일치 항목

- **deleteMember role 배열 확장**: L401 `if (!["lead", "member", "inactive"].includes(profile.role))` — 기존 `["lead", "member"]`에서 `"inactive"` 추가. 비활성화된 회원도 삭제 가능. (설계서 3-B2 일치)
- **에러 메시지 유지**: L402 `return { error: "수강생과 관리자는 삭제할 수 없습니다." }` — 기존 에러 메시지 그대로 유지. (설계서 3-B2 일치)
- **한 줄 수정**: diff 확인 결과, admin.ts에서 변경된 줄이 L401 한 줄뿐. (TASK.md B2 "한 줄 수정" 일치)

### 금지 사항 검증

- **admin role 삭제 허용 여부**: `["lead", "member", "inactive"]` 배열에 `"admin"` 없음. admin은 삭제 차단. 확인 완료.
- **student role 삭제 허용 여부**: `["lead", "member", "inactive"]` 배열에 `"student"` 없음. student는 삭제 차단. 확인 완료.
- **assistant role 삭제 허용 여부**: `["lead", "member", "inactive"]` 배열에 `"assistant"` 없음. assistant는 삭제 차단. 확인 완료.
- **다른 삭제 로직 변경**: deleteMember 함수의 나머지 로직(profiles 삭제 → auth.users 삭제 → revalidatePath) 변경 없음. 확인 완료.

### 불일치 항목

- 없음

### 수정 필요

- 없음

---

## T2-fix. QA_SYSTEM_PROMPT 답변 구조 교체 — Match Rate: 100%

### 일치 항목

- **기존 "핵심:/정리하면:" 구조 제거**: diff에서 아래 기존 구조가 삭제됨을 확인.
  - `**핵심:** [1-2문장으로 질문의 답]`
  - `[상세 설명 - 강의 자료 기반]`
  - `**정리하면:**`
  - `- [실행 가능한 포인트 1]`
  - `- [실행 가능한 포인트 2]`
  - `"핵심:"으로 반드시 시작해라.` / `"정리하면:"으로 반드시 끝내라.`
  (설계서 3-T2-fix 일치)

- **새 답변 구조 규칙 삽입**: L95-101 `[답변 구조]` 섹션이 TASK.md에서 지정한 내용과 정확히 일치.
  - L96: `"핵심:", "정리하면:", "요약:" 같은 볼드 키워드로 시작하거나 끝내지 마라` (일치)
  - L97: `바로 본론부터 시작해라. 인사나 서론 없이.` (일치)
  - L98: `질문이 여러 개여도 Q1/Q2 번호 매기지 마라. 자연스럽게 이어서 써라.` (일치)
  - L99: `마지막에 앞에서 한 말을 불릿으로 반복 요약하지 마라. 자연스럽게 끝내라.` (일치)
  - L100: `용어를 나열할 때 교과서처럼 빠짐없이 순서대로 쓰지 마라. 핵심적인 것만 언급해라.` (일치)

### 금지 사항 검증

- **마크다운 포맷팅 규칙 수정 여부**: L111-119 마크다운 포맷팅 규칙 블록 — diff에서 변경 없음. 확인 완료.
- **AI 상투어 금지 목록 수정 여부**: L135-144 AI 상투어 금지 목록 — diff에서 변경 없음. 확인 완료.
- **정보공유 프롬프트(route.ts) 수정 여부**: `git diff main --name-only` 결과에 route.ts 파일 없음. 미수정 확인.

### 불일치 항목

- 없음

### 수정 필요

- 없음

---

## T4-fix. QA_SYSTEM_PROMPT 말투 요체 변경 — Match Rate: 100%

### 일치 항목

- **말투 규칙 교체**: L102-109 `[말투]` 섹션이 TASK.md에서 지정한 내용과 정확히 일치.
  - L103: `요체(~요, ~거든요, ~이에요) 기본` (일치)
  - L104: `"~입니다/~합니다" 딱딱한 존댓말 금지` (일치)
  - L105: `"안녕하세요!", "도움이 되셨길 바랍니다" 같은 챗봇 인사 금지` (일치)
  - L106: `이모지 금지` (일치)
  - L107: `실무 선배가 후배한테 알려주는 톤. 친절하되 가볍지 않게.` (일치)

- **어미 다양화 규칙 요체 기반 업데이트**: L125-128 어미 다양화 규칙이 요체 기반으로 변경.
  - 기존: `~다, ~거든요, ~세요, ~죠, ~한데요` → 변경: `~요, ~거든요, ~이에요, ~죠, ~한데요` (일치)
  - 기존: `"~다" → "~거든요"` → 변경: `"~요" → "~거든요" → "~이에요" → "~한데요"` (일치)
  - 기존: `단정형(~다, ~거든) 위주` → 변경: `요체 기반이되 다양한 어미를 섞어라` (일치)

- **문장 리듬 예시 요체 수정**: L131-133 문장 리듬 예시 변경.
  - 기존: `"결과가 나왔다."` → 변경: `"결과가 나왔어요."` (일치)
  - 기존: `"진짜 됩니다."` → 변경: `"진짜 돼요."` (일치)

- **톤 레퍼런스 요체 수정**: L157-159 톤 레퍼런스의 좋은 예시 변경.
  - 기존: `"이유는 세 가지다."` → 변경: `"이유는 세 가지예요."` (일치)
  - 기존: `"데이터를 봐야 한다."` → 변경: `"데이터를 봐야 해요."` (일치)
  - 기존: `"자주 나오는 질문이다. ... 답이 안 나온다. ... 3초 시청률이거든요."` → 변경: `"자주 나오는 질문이에요. ... 답이 안 나오거든요. ... 3초 시청률이에요."` (일치)

- **답변 예시 교체 (좋은 예)**: L164-170 TASK.md에서 지정한 네이버 쇼핑 예시로 교체.
  - `네이버 쇼핑 입점 자체는 Meta 학습이랑 별개예요.` (일치)
  - `네이버 쇼핑에서 발생하는 구매는 자사몰 픽셀에 안 쌓이거든요.` (일치)
  - `근데 진짜 고민해야 할 건 기존에 SA나 GFA로 보내던 트래픽이에요.` (일치)
  - `매출 분산 기준으로 판단하시면 돼요.` (일치)

- **답변 예시 교체 (나쁜 예)**: L172-179 TASK.md에서 지정한 나쁜 예시로 교체.
  - `**핵심:** 네이버 쇼핑 입점은 Meta 학습과 무관하다.` (일치)
  - `Q1부터 짚고 가면,` (일치)
  - `**정리하면:**` + 불릿 요약 패턴 (일치)

- **셀프 검수 요체 규칙 수정**: L185 셀프 검수 3번 항목 변경.
  - 기존: `"~요"로만 끝나는 문단이 2개 이상 연속 → 톤 전환` → 변경: `같은 어미 패턴이 2문단 이상 연속 → 어미 변주` (일치)

### 금지 사항 검증

- **마크다운 포맷팅 규칙 수정 여부**: L111-119 — diff에서 이 영역 변경 없음. 확인 완료.
- **AI 상투어 금지 목록 수정 여부**: L135-144 — diff에서 이 영역 변경 없음. 확인 완료.
- **숫자/범위 표기 규칙 수정 여부**: L151-155 — diff에서 이 영역 변경 없음. 확인 완료.
- **경험담 톤 수정 여부**: L146-149 — diff에서 이 영역 변경 없음. 확인 완료.
- **정보공유 프롬프트(route.ts) 수정 여부**: `git diff main --name-only` 결과에 route.ts 파일 없음. 미수정 확인.
- **CONSUMER_CONFIGS 변경 여부**: qa config(L196-212), chatbot config(L264-280), 기타 consumer — diff에서 변경 없음. 확인 완료.
- **RAG 로직 변경 여부**: `searchChunks`, `searchChunksByEmbedding`, `buildSearchResults`, `buildContext`, `generate` 함수 — diff에서 변경 없음. 확인 완료.

### 불일치 항목

- 없음

### 수정 필요

- 없음

---

## 전체 금지 사항 검증 (Cross-cutting)

| 검증 항목 | 결과 | 근거 |
|---|---|---|
| route.ts (정보공유 프롬프트) 미수정 | PASS | `git diff main --name-only`에 route.ts 없음 |
| admin/student/assistant role 삭제 차단 유지 | PASS | `["lead", "member", "inactive"]`에 해당 role 없음 |
| middleware.ts 미수정 | PASS | `git diff main --name-only`에 middleware.ts 없음 |
| 마크다운 포맷팅 규칙 미수정 | PASS | diff에서 L111-119 영역 변경 없음 |
| AI 상투어 금지 목록 미수정 | PASS | diff에서 L135-144 영역 변경 없음 |
| 수정 파일 범위 | PASS | 변경 파일 4개: TASK.md, signup/page.tsx, admin.ts, knowledge.ts — 설계서 범위 내 |

---

## 변경 파일 요약

| 파일 | 변경 라인 수 | 작업 |
|---|---|---|
| `src/app/(auth)/signup/page.tsx` | +3 / -2 | B1: authError 조건 수정 + optional chaining |
| `src/actions/admin.ts` | +1 / -1 | B2: inactive role 삭제 허용 |
| `src/lib/knowledge.ts` | +49 / -49 | T2-fix + T4-fix: 답변 구조 + 말투 + few-shot 교체 |
| `TASK.md` | (작업 명세) | 작업 정의서 |

---

## 빌드 검증

- **tsc + lint + build**: 이미 통과 확인 (사전 검증 완료)

---

## Match Rate 산출

| 작업 | 설계 항목 수 | 일치 | 불일치 | Match Rate |
|---|---|---|---|---|
| B1 | 5 | 5 | 0 | 100% |
| B2 | 3 | 3 | 0 | 100% |
| T2-fix | 6 | 6 | 0 | 100% |
| T4-fix | 8 | 8 | 0 | 100% |
| **전체** | **22** | **22** | **0** | **100%** |
