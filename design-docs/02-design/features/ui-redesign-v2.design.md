# UI 리디자인 V2 디자인 문서

## 1. 디자인 토큰 정의

### 1.1 컬러 시스템
```css
:root {
  /* Primary Brand Colors */
  --color-primary: #FF5757;              /* 코랄 레드 (포인트) */
  --color-primary-light: #FF7676;        /* Primary 10% 밝게 */
  --color-primary-dark: #E84444;         /* Primary 10% 어둡게 */
  
  /* Neutral Colors */
  --color-text: #37352f;                 /* 짙은 올리브 (메인 텍스트) */
  --color-text-secondary: #73726a;       /* 보조 텍스트 */
  --color-text-muted: #a8a29e;          /* 비활성 텍스트 */
  
  /* Background Colors */
  --color-background: #f7f6f5;          /* 따뜻한 오프화이트 */
  --color-background-soft: #faf9f8;     /* 더 밝은 배경 */
  --color-card: #ffffff;                 /* 카드 배경 */
  --color-card-hover: #fafafa;          /* 카드 호버 */
  
  /* Border & Lines */
  --color-border: #e5e7eb;              /* 기본 보더 */
  --color-border-light: #f3f4f6;        /* 연한 보더 */
  --color-border-focus: #FF5757;        /* 포커스 보더 */
  
  /* Status Colors */
  --color-success: #10b981;             /* 답변 완료 */
  --color-warning: #f59e0b;             /* 답변 대기 */
  --color-error: #ef4444;               /* 오류 */
  --color-info: #3b82f6;                /* 정보 */
  
  /* Overlay & Shadow */
  --color-overlay: rgba(55, 53, 47, 0.1);
  --color-shadow: rgba(55, 53, 47, 0.08);
}
```

### 1.2 타이포그래피
```css
/* Fonts */
--font-primary: 'Pretendard Variable', -apple-system, BlinkMacSystemFont, system-ui, sans-serif;
--font-accent: 'Space Grotesk', monospace;

/* Font Sizes (clamp for responsive) */
--text-xs: clamp(0.75rem, 1vw, 0.875rem);     /* 12-14px */
--text-sm: clamp(0.875rem, 1.2vw, 1rem);      /* 14-16px */
--text-base: clamp(1rem, 1.4vw, 1.125rem);    /* 16-18px */
--text-lg: clamp(1.125rem, 1.6vw, 1.25rem);   /* 18-20px */
--text-xl: clamp(1.25rem, 2vw, 1.5rem);       /* 20-24px */
--text-2xl: clamp(1.5rem, 2.5vw, 1.875rem);   /* 24-30px */
--text-3xl: clamp(1.875rem, 3vw, 2.25rem);    /* 30-36px */

/* Font Weights */
--font-normal: 400;
--font-medium: 500;
--font-semibold: 600;
--font-bold: 700;

/* Line Heights */
--leading-tight: 1.25;
--leading-normal: 1.5;
--leading-relaxed: 1.75;
```

### 1.3 간격 & 크기
```css
/* Spacing (8pt grid) */
--space-1: 0.25rem;    /* 4px */
--space-2: 0.5rem;     /* 8px */
--space-3: 0.75rem;    /* 12px */
--space-4: 1rem;       /* 16px */
--space-5: 1.25rem;    /* 20px */
--space-6: 1.5rem;     /* 24px */
--space-8: 2rem;       /* 32px */
--space-10: 2.5rem;    /* 40px */
--space-12: 3rem;      /* 48px */
--space-16: 4rem;      /* 64px */
--space-20: 5rem;      /* 80px */

/* Container Widths */
--container-xs: 20rem;      /* 320px */
--container-sm: 24rem;      /* 384px */
--container-md: 28rem;      /* 448px */
--container-lg: 32rem;      /* 512px */
--container-xl: 36rem;      /* 576px */
--container-max: 80rem;     /* 1280px */
```

### 1.4 라운딩 & 그림자
```css
/* Border Radius */
--radius-sm: 0.375rem;     /* 6px */
--radius-md: 0.5rem;       /* 8px */
--radius-lg: 0.75rem;      /* 12px */
--radius-xl: 1rem;         /* 16px */
--radius-full: 9999px;

/* Shadows */
--shadow-sm: 0 1px 2px rgba(55, 53, 47, 0.05);
--shadow-md: 0 4px 6px rgba(55, 53, 47, 0.07);
--shadow-lg: 0 10px 15px rgba(55, 53, 47, 0.1);
--shadow-xl: 0 20px 25px rgba(55, 53, 47, 0.1);
```

### 1.5 애니메이션 & 트랜지션
```css
/* Transitions */
--transition-fast: all 0.15s ease-out;
--transition-normal: all 0.3s ease-out;
--transition-slow: all 0.5s ease-out;

/* Easing Functions */
--ease-in-out: cubic-bezier(0.4, 0, 0.2, 1);
--ease-out: cubic-bezier(0, 0, 0.2, 1);
--ease-in: cubic-bezier(0.4, 0, 1, 1);
```

## 2. 반응형 Breakpoints

```css
/* Mobile First Approach */
--breakpoint-sm: 640px;    /* 모바일 → 태블릿 */
--breakpoint-md: 768px;    /* 태블릿 → 작은 데스크탑 */
--breakpoint-lg: 1024px;   /* 작은 데스크탑 → 큰 데스크탑 */
--breakpoint-xl: 1280px;   /* 큰 데스크탑 → 초대형 */

/* Grid System */
모바일: 1열 (full-width)
태블릿: 2열 (gap-4)
데스크탑: 3열 (gap-6)
와이드: 4열 (gap-8)
```

## 3. 페이지별 레이아웃 설계

### 3.1 로그인/회원가입 페이지
```
┌─────────────────────────────────────┐
│ [로고]          [언어 선택] [테마]   │
├─────────────────────────────────────┤
│                                     │
│    ┌───────────────────────┐       │
│    │                       │       │
│    │   로그인 폼 카드      │       │
│    │   - 이메일 입력       │       │
│    │   - 비밀번호 입력     │       │
│    │   - 로그인 버튼       │       │
│    │   - 회원가입 링크     │       │
│    │                       │       │
│    └───────────────────────┘       │
│                                     │
│   "Q&A로 성장하는 학습 커뮤니티"   │
│                                     │
└─────────────────────────────────────┘

특징:
- 중앙 정렬 카드 형태
- 따뜻한 배경색 (#f7f6f5)
- 로고와 캐치프레이즈로 브랜드 강조
- Space Grotesk 폰트로 영문 포인트
```

### 3.2 홈 페이지 (공지 + 최근 Q&A)
```
┌─────────────────────────────────────┐
│ [로고] [홈] [Q&A] [내질문] [프로필] │
├─────────────────────────────────────┤
│                                     │
│ 🔍 [────── 검색바 ──────] [필터]   │
│                                     │
│ 📢 공지사항                        │
│ ┌─────────────────────────────────┐ │
│ │ [중요] 서비스 업데이트 안내     │ │
│ │ 2024.02.04                      │ │
│ └─────────────────────────────────┘ │
│                                     │
│ 💬 최근 Q&A                        │
│ ┌─────┐ ┌─────┐ ┌─────┐           │
│ │ Q&A │ │ Q&A │ │ Q&A │ → 푸딩캠프 │
│ │ 카드│ │ 카드│ │ 카드│   그리드  │
│ └─────┘ └─────┘ └─────┘   스타일  │
│ [더보기 →]                         │
│                                     │
└─────────────────────────────────────┘

특징:
- 상단 네비게이션 바 (사이드바 제거)
- 히어로 검색바
- 공지사항 카드
- Q&A 카드 그리드 미리보기
- 푸딩캠프 스타일 카드 레이아웃
```

### 3.3 Q&A 리스트 페이지
```
┌─────────────────────────────────────┐
│ [로고] [홈] [Q&A] [내질문] [프로필] │
├─────────────────────────────────────┤
│                                     │
│ 🔍 [────── 검색바 ──────] [필터]   │
│                                     │
│ [전체] [내 질문] 탭                │
│                                     │
│ 🏷️ [전체] [리액트] [Next.js] [...] │
│                                     │
│ ┌─────┐ ┌─────┐ ┌─────┐           │
│ │ 📝  │ │ 💬  │ │ ✅  │           │
│ │제목 │ │제목 │ │제목 │           │
│ │[태그│ │[태그│ │[태그│           │
│ │ 대기│ │ 완료│ │ 완료│           │
│ │1시간│ │2일  │ │1주  │           │
│ └─────┘ └─────┘ └─────┘           │
│                                     │
│        [페이지네이션]              │
│         1 2 3 ... 10               │
│                                     │
└─────────────────────────────────────┘

특징:
- 탭 형태의 전체/내 질문 분류
- 카테고리 태그 칩 필터
- 카드 그리드 (반응형 1/2/3열)
- 상태 배지 (답변대기/완료)
- 인프런 스타일 상태 표시
```

### 3.4 Q&A 상세 페이지
```
┌─────────────────────────────────────┐
│ [로고] [홈] [Q&A] [내질문] [프로필] │
├─────────────────────────────────────┤
│                                     │
│ 🏠 홈 > Q&A > 리액트 질문          │
│                                     │
│ ┌─────────────────────────────────┐ │
│ │ 📝 리액트 useState 사용법?      │ │
│ │                                 │ │
│ │ [리액트] [useState]    ⏰ 2시간  │ │
│ │                                 │ │
│ │ 질문 내용이 여기에...           │ │
│ │                                 │ │
│ │ 👤 김학생 • 답변대기            │ │
│ └─────────────────────────────────┘ │
│                                     │
│ 💬 답변 (2개)                      │
│                                     │
│ ┌─────────────────────────────────┐ │
│ │ ✅ 답변 1                       │ │
│ │ 답변 내용...                    │ │
│ │ 👨‍🏫 이선생 • 1시간 전          │ │
│ └─────────────────────────────────┘ │
│                                     │
│ [답변 작성 폼]                      │
│                                     │
└─────────────────────────────────────┘

특징:
- 브레드크럼 네비게이션
- 질문/답변 카드 구분
- 작성자 아바타 + 역할 표시
- 타임스탬프
- 상태 아이콘
```

### 3.5 질문 작성 페이지
```
┌─────────────────────────────────────┐
│ [로고] [홈] [Q&A] [내질문] [프로필] │
├─────────────────────────────────────┤
│                                     │
│ 📝 새 질문 작성                    │
│                                     │
│ ┌─────────────────────────────────┐ │
│ │                                 │ │
│ │ 제목 입력                       │ │
│ │ [────────────────────────]      │ │
│ │                                 │ │
│ │ 카테고리                        │ │
│ │ [리액트 ▼]                      │ │
│ │                                 │ │
│ │ 내용 작성                       │ │
│ │ ┌─────────────────────────────┐ │ │
│ │ │                             │ │ │
│ │ │ 에디터 영역                 │ │ │
│ │ │                             │ │ │
│ │ └─────────────────────────────┘ │ │
│ │                                 │ │
│ │ [취소] [질문 등록]              │ │
│ │                                 │ │
│ └─────────────────────────────────┘ │
│                                     │
└─────────────────────────────────────┘

특징:
- 폼 기반 카드 레이아웃
- 드롭다운 카테고리 선택
- 마크다운 에디터
- 명확한 CTA 버튼
```

### 3.6 관리자 대시보드
```
┌─────────────────────────────────────┐
│ [로고] [대시보드] [회원] [답변관리] │
├─────────────────────────────────────┤
│                                     │
│ 📊 대시보드 개요                   │
│                                     │
│ ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐   │
│ │ 총   │ │ 답변 │ │ 답변 │ │ 월   │ │
│ │질문수│ │ 대기│ │ 완료│ │ 신규│ │
│ │ 127 │ │  8  │ │ 119 │ │ 24  │ │
│ └─────┘ └─────┘ └─────┘ └─────┘   │
│                                     │
│ 📈 주간 통계 차트                  │
│ ┌─────────────────────────────────┐ │
│ │      █                          │ │
│ │   █  ██    █                    │ │
│ │ █ ██ ████ ███ █                 │ │
│ │ 월 화 수 목 금 토 일            │ │
│ └─────────────────────────────────┘ │
│                                     │
│ 🔥 답변 대기 중인 질문              │
│ • 리액트 Hook 질문 (2시간 전)     │ │
│ • CSS 레이아웃 문제 (4시간 전)     │ │
│                                     │
└─────────────────────────────────────┘

특징:
- 관리자 전용 네비게이션
- 카드 기반 지표
- 시각화 차트
- 우선순위 큐
```

## 4. 컴포넌트 구조

### 4.1 Layout Components
```
📁 components/layout/
├── 📄 Header.tsx              # 상단 네비게이션
├── 📄 Navigation.tsx          # 메인 네비게이션
├── 📄 Breadcrumb.tsx          # 브레드크럼
├── 📄 MobileNav.tsx           # 모바일 네비게이션
└── 📄 Footer.tsx              # 하단 정보

Header 구조:
- 로고 (좌측)
- 네비게이션 메뉴 (중앙)
- 검색, 프로필 (우측)
- 반응형: 모바일에서 햄버거 메뉴
```

### 4.2 Card Components
```
📁 components/cards/
├── 📄 QuestionCard.tsx        # Q&A 카드 (그리드용)
├── 📄 NoticeCard.tsx          # 공지사항 카드
├── 📄 StatsCard.tsx           # 통계 카드
└── 📄 UserCard.tsx            # 사용자 카드

QuestionCard 구조:
┌─────────────────────────┐
│ 📝 질문 제목            │
│                         │
│ [태그1] [태그2]         │
│                         │
│ 👤 작성자 • 답변대기    │
│ ⏰ 2시간 전             │
└─────────────────────────┘

Props:
- title: string
- content: string  
- tags: string[]
- author: User
- status: 'pending' | 'answered'
- createdAt: Date
```

### 4.3 Form Components
```
📁 components/forms/
├── 📄 SearchForm.tsx          # 검색 폼
├── 📄 LoginForm.tsx           # 로그인 폼
├── 📄 QuestionForm.tsx        # 질문 작성 폼
├── 📄 AnswerForm.tsx          # 답변 작성 폼
└── 📄 FilterForm.tsx          # 필터 폼
```

### 4.4 Shared Components
```
📁 components/shared/
├── 📄 Button.tsx              # 통일된 버튼
├── 📄 Badge.tsx               # 상태 배지
├── 📄 TagChip.tsx             # 카테고리 태그
├── 📄 Avatar.tsx              # 아바타 이미지
├── 📄 LoadingSpinner.tsx      # 로딩 상태
└── 📄 EmptyState.tsx          # 빈 상태 화면

Button 변형:
- Primary (포인트 컬러)
- Secondary (회색)
- Ghost (투명)
- Outline (테두리만)

Badge 변형:
- Success (답변 완료)
- Warning (답변 대기)
- Info (공지)
- Error (오류)
```

## 5. 인터랙션 & 애니메이션

### 5.1 Hover Effects
```css
/* Card Hover */
.card {
  transition: var(--transition-normal);
  transform: translateY(0);
}

.card:hover {
  transform: translateY(-2px);
  box-shadow: var(--shadow-lg);
}

/* Button Hover */
.btn-primary:hover {
  background: var(--color-primary-dark);
  transform: translateY(-1px);
}

/* Link Hover */
.nav-link:hover {
  color: var(--color-primary);
  text-decoration: underline;
}
```

### 5.2 Focus States
```css
/* Form Focus */
.input:focus {
  outline: 2px solid var(--color-primary);
  outline-offset: 2px;
  border-color: transparent;
}

/* Card Focus (키보드 접근) */
.card-link:focus {
  outline: 2px solid var(--color-primary);
  outline-offset: 4px;
}
```

### 5.3 Loading States
```css
/* Skeleton Loading */
.skeleton {
  background: linear-gradient(
    90deg,
    #f0f0f0 25%,
    #e0e0e0 50%,
    #f0f0f0 75%
  );
  background-size: 200% 100%;
  animation: loading 1.5s infinite;
}

@keyframes loading {
  0% { background-position: 200% 0; }
  100% { background-position: -200% 0; }
}

/* Fade In Animation */
.fade-in {
  animation: fadeIn 0.3s ease-out;
}

@keyframes fadeIn {
  from { opacity: 0; transform: translateY(8px); }
  to { opacity: 1; transform: translateY(0); }
}
```

### 5.4 Micro Interactions
```css
/* Button Press */
.btn:active {
  transform: scale(0.98);
}

/* Tag Chip Interaction */
.tag-chip {
  cursor: pointer;
  transition: var(--transition-fast);
}

.tag-chip:hover {
  background: var(--color-primary);
  color: white;
}

/* Search Bar Focus */
.search-bar:focus-within {
  box-shadow: 0 0 0 4px rgba(255, 87, 87, 0.1);
}
```

## 6. 접근성 (Accessibility)

### 6.1 색상 대비
- 텍스트: #37352f on #ffffff = 12.6:1 (AAA 등급)
- 보조 텍스트: #73726a on #ffffff = 5.1:1 (AA 등급)
- 포인트 컬러: #FF5757 on #ffffff = 4.7:1 (AA 등급)

### 6.2 키보드 네비게이션
- Tab 순서: 로고 → 네비게이션 → 검색 → 카드 → 페이지네이션
- Skip Links: "메인 콘텐츠로 건너뛰기"
- Focus Visible: 명확한 아웃라인

### 6.3 스크린 리더
```html
<!-- ARIA 레이블 -->
<button aria-label="질문 검색">🔍</button>
<div role="status" aria-live="polite">로딩 중...</div>
<nav aria-label="메인 네비게이션">

<!-- 시맨틱 HTML -->
<main>
<article> (Q&A 카드)
<aside> (사이드바)
<section> (섹션별 그룹)
```

## 7. 성능 최적화

### 7.1 이미지 최적화
- Next.js Image 컴포넌트 사용
- 아바타: 40x40, 80x80 (2x)
- 로고: SVG 우선
- 백그라운드: WebP 형태

### 7.2 폰트 로딩
```css
/* 폰트 디스플레이 설정 */
@font-face {
  font-family: 'Pretendard Variable';
  src: url('...') format('woff2');
  font-display: swap;
}
```

### 7.3 코드 스플리팅
- 페이지별 청크 분리
- 동적 import for 관리자 페이지
- 라이브러리 번들 최적화

## 8. 다크 모드 대응

### 8.1 다크 테마 컬러
```css
[data-theme="dark"] {
  --color-background: #1a1a1a;
  --color-card: #2a2a2a;
  --color-text: #e4e4e4;
  --color-border: #404040;
  /* 포인트 컬러는 동일 유지 */
  --color-primary: #FF5757;
}
```

### 8.2 자동 감지
```javascript
// 시스템 설정 감지
const prefersColorScheme = window.matchMedia(
  '(prefers-color-scheme: dark)'
);
```

---

이 디자인 문서는 푸딩캠프의 따뜻한 톤앤매너와 인프런의 구조적 명확성을 결합하여 교육 서비스에 적합한 Q&A 플랫폼을 만들기 위한 종합적인 설계서입니다.